Blockly.Python['move'] = function(block) {
  var number_twist_value = block.getFieldValue('twist value');
  var value_twist_value = Blockly.Python.valueToCode(block, 'twist value', Blockly.Python.ORDER_ATOMIC);
  // TODO: Assemble Python into code variable.
  var code = '...\n';
  return code;
};